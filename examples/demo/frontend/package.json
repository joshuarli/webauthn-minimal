{
  "name": "webauthn-demo-frontend",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "check": "tsc --noEmit",
    "build": "npm run check && esbuild src/main.tsx --bundle --minify --outfile=dist/bundle.js --loader:.tsx=tsx"
  },
  "dependencies": {
    "preact": "^10.20.1"
  },
  "devDependencies": {
    "esbuild": "^0.28.0",
    "preact": "^10.20.1",
    "typescript": "^5.4.5"
  }
}
